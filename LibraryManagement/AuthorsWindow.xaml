<Window x:Class="LibraryManagement.AuthorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Список авторов" FontSize="16" FontWeight="Bold"/>

        <DataGrid x:Name="dgAuthors" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat='dd.MM.yyyy'}" Width="120"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnAddAuthor" Content="Добавить" Width="80" Margin="5" Click="btnAddAuthor_Click"/>
            <Button x:Name="btnEditAuthor" Content="Изменить" Width="80" Margin="5" Click="btnEditAuthor_Click"/>
            <Button x:Name="btnDeleteAuthor" Content="Удалить" Width="80" Margin="5" Click="btnDeleteAuthor_Click"/>
        </StackPanel>
    </Grid>
</Window>